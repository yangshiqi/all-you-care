name: ai · compress
on:
  schedule:
    - cron: '10 */3 * * *'      # every 3h at :10
  workflow_dispatch: {}

jobs:
  compress:
    uses: ./.github/workflows/_pipeline.yml
    with: { channel: ai, step: compress }
    secrets: inherit
