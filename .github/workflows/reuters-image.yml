name: ai · reuters-image
on:
  schedule:
    - cron: '0 23 * * *'       # 07:00 Asia/Shanghai, refresh before 08:30 morning paper
  workflow_dispatch: {}

jobs:
  reuters:
    uses: ./.github/workflows/_pipeline.yml
    with: { channel: ai, step: reutersImage }
    secrets: inherit
