name: infra · compress
on:
  # Scheduled by cron-job.org (see pipeline/scripts/cron-fallback.md).
  workflow_dispatch: {}

jobs:
  compress:
    uses: ./.github/workflows/_pipeline.yml
    with:
      channel: infra
      step: compress
    secrets: inherit
