name: ai · tags
on:
  schedule:
    - cron: '0 1 * * *'         # 09:00 Asia/Shanghai, runs after morning publish
  workflow_dispatch: {}

jobs:
  tags:
    uses: ./.github/workflows/_pipeline.yml
    with: { channel: ai, step: tags }
    secrets: inherit
