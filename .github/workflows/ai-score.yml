name: ai · score
on:
  schedule:
    - cron: '40 */3 * * *'      # every 3h at :40, 30min after compress
  workflow_dispatch: {}

jobs:
  score:
    uses: ./.github/workflows/_pipeline.yml
    with: { channel: ai, step: score }
    secrets: inherit
